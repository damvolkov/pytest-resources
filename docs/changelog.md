# Changelog

This page mirrors the repository changelog. See
<https://github.com/damvolkov/pytest-resources/blob/main/CHANGELOG.md> for the canonical file.

## [0.1.0]

Initial release: the `resources` session fixture, `FileType`, a pluggable loader table with
e-serde default codecs and a stdlib fallback, the `pytest_resource_loaders` hook, and
`--resources-root` / `resources_root` configuration.
